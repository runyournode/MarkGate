DOCLING_SERVE_MAX_SYNC_WAIT=10  # timeout global, a finetuner !

DOCLING_SERVE_ENABLE_UI=true  # todo: dev: remove in prod
DOCLING_SERVE_ENABLE_REMOTE_SERVICES=true  # allow calls to vllm
NVIDIA_VISIBLE_DEVICES=all




#DOCLING_SERVE_ARTIFACTS_PATH=/opt/app-root/src/.cache/docling/models  # default value, make sure all models are already downloaded here